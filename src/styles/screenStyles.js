import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#fff",
    alignItems: "center",
    justifyContent: "center",
  },
  pokedexContainer: {
    backgroundColor: "#fff",
    flex: 1,

    // alignItems: "center",
    // justifyContent: "center",
  },
});
